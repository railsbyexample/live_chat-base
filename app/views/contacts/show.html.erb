<%= react_component 'contacts/Show',
                    contact: render(
                      partial: 'contacts/contact',
                      formats: %i[json],
                      locals: { contact: @contact, included: %i[messages] }
                    )
%>
