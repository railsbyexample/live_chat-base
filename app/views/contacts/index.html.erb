<%= react_component 'contacts/Index',
                    contacts: @contacts.to_json(include: { sender: { methods: %i[name gravatar_url] }, receiver: { methods: %i[name gravatar_url] } }, methods: %i[last_message]),
                    current_user_id: current_user.id,
                    add_user_icon: image_url('add_user_icon.svg'),
                    delete_icon: image_url('delete_icon.svg')
%>
