<%= react_component 'contacts/New',
                    unconfirmed_contacts: render(
                      partial: 'contacts/contacts',
                      formats: [:json],
                      locals: { contacts: @unconfirmed_contacts }
                    )
%>
