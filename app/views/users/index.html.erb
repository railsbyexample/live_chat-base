<%= react_component 'users/Index',
                    users: @confirmed_users.to_json(methods: %i[name gravatar_url]),
                    current_user_id: current_user.id,
                    add_user_icon: image_url('add_user_icon.svg'),
                    delete_icon: image_url('delete_icon.svg')
%>
