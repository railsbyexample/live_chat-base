<%= react_component 'conversations/Show',
                    messages: @conversation.messages.to_json(include: { user: { methods: %i[name gravatar_url] } }),
                    current_user_id: current_user.id
%>
<!-- <div class="container" id="conversations-show"> -->
  <!-- <h6>Conversation with //= other_user(@conversation).name %></h6>

  <div hidden="true">
    <div id="message-template">
      //= render @message %>
    </div>
  </div>

  <div id="messages" class="list-group" data-conversation-id="//= @conversation.id %>">
    //= render @conversation.messages %>
  </div>

  //= form_for @message, url: '#' do |f| %>
    <div class="input-group">
      //= f.text_field :body, class: 'form-control' %>
      <span class="input-group-btn">
        //= f.submit "Post", class: 'btn btn-primary' %>
      </span>
    </div>
  // end %> -->
<!-- </div> -->
